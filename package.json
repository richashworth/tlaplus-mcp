{
  "name": "tlaplus-mcp",
  "version": "0.1.0",
  "description": "MCP server for TLA+ toolchain (TLC, SANY, PlusCal, TLATeX)",
  "type": "module",
  "bin": {
    "tlaplus-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc && rm -rf dist/templates && cp -r src/templates dist/templates",
    "start": "node dist/index.js",
    "dev": "rm -rf dist/templates && cp -r src/templates dist/templates && tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
